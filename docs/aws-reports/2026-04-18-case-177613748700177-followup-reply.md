# AWS case 177613748700177 — follow-up reply (Apr 18 2026)

**Account:** `767697632458`  
**Case:** `177613748700177`  
**In response to:** AWS / Vignesh P. follow-up email dated Apr 16 2026  
**Related sweep report:** `docs/aws-reports/2026-04-18-followup-sweep-767697632458.md`

---

## Context for operator before sending

The Apr 16 email from AWS (Vignesh P.) acknowledged our prior actions but flagged:

> "I could still see multiple suspicious EC2 Security groups in all the active regions."

Our Apr 18 sweep identified and deleted:
- `sec-hhs` across 16 regions (attacker-planted, no instances attached)
- 13 region-named key pairs (attacker-staged across 13 regions)
- 8 orphaned `launch-wizard` SGs in `us-east-1` including one with world-open RDP/MSSQL
- Revoked world-open TCP/UDP ingress rules from the `us-west-1` default SG

AWS also asked us to confirm:
- Location we are accessing the account from
- List of IAM users with access and their locations

Fill in the two bracketed fields (`[YOUR CITY, COUNTRY]`) before sending.

---

## Message to paste into AWS support case 177613748700177

```
Hello Vignesh,

Thank you for the continued follow-up on case 177613748700177.

We have now completed the security group review across all active regions and
taken the following actions on 2026-04-18:

──────────────────────────────────────────────
1. SECURITY GROUPS — FULLY REMEDIATED
──────────────────────────────────────────────

a) "sec-hhs" group (attacker-planted):
   We found a non-default security group named "sec-hhs" identically replicated
   across 16 regions. It had world-open ingress on ports 22, 80, and 443 with
   all-traffic egress — consistent with attacker pre-staging. No production
   instances were attached to any of them. All 16 have been deleted.

   Regions: ap-northeast-1, ap-northeast-2, ap-south-1, ap-southeast-1,
   ap-southeast-2, ca-central-1, eu-central-1, eu-north-1, eu-west-1,
   eu-west-2, eu-west-3, sa-east-1, us-east-1, us-east-2, us-west-1, us-west-2.

b) Orphaned launch-wizard groups (us-east-1):
   8 orphaned launch-wizard security groups with no attached instances were
   deleted, including one with world-open RDP (port 3389) and MSSQL (port 1433)
   ingress. These were confirmed to have no attached running instances before
   deletion.

c) us-west-1 default SG:
   The default SG in us-west-1 had world-open TCP/UDP 0-65535 ingress rules.
   Default SGs cannot be deleted, so the offending ingress rules were revoked
   individually.

The account now has no unauthorized non-default security groups. The only
remaining non-default groups are operator-confirmed production security groups
actively in use by named instances.

──────────────────────────────────────────────
2. KEY PAIRS — ADDITIONAL CLEANUP
──────────────────────────────────────────────

In addition to the previously reported deletion of "buatbelisdfgmsobilbaim",
we also identified and deleted 13 key pairs whose names exactly matched their
host region (e.g. "eu-west-1" in eu-west-1). This is consistent with attacker
pre-staging across regions. No instances were launched with any of them.

──────────────────────────────────────────────
3. ACCOUNT ACCESS DETAILS (as requested)
──────────────────────────────────────────────

Location: We are accessing the account from [YOUR CITY, COUNTRY].
We are not using a VPN for routine account access.

IAM users with active access keys in this account:
- edgar_aws_billing_automation — Cost Explorer access, us-east-1
- truesight_app — S3 access, us-east-1
- truesight_model — no recent recorded use
- database_backup — EC2 access only (created post-incident for backup automation)

All of the above are legitimate operator accounts. No IAM users were created
by the attacker — the compromised key (AKIAIQYX7Z65F25NMY6A) was a root key
and is confirmed removed from the account.

──────────────────────────────────────────────
4. PRIOR STEPS — CONFIRMATION
──────────────────────────────────────────────

For completeness, confirming the steps from the original notice:
- Root password: changed 2026-04-15
- Root MFA: enabled and active (confirmed in credential report: mfa_active = true)
- CloudTrail reviewed: unauthorized root API usage from 45.61.128.156 using
  AKIAIQYX7Z65F25NMY6A identified and documented; key confirmed removed

──────────────────────────────────────────────

We believe the account is now fully remediated per your request. We would
appreciate confirmation that the temporary service limitations can be lifted
and that this case can be progressed toward resolution.

Please let us know if any further information or evidence is needed.

Thank you,
Gary Teh
GetData Inc / TrueSight DAO
Account: 767697632458
```

---

## After sending

- Record the date/time sent in a note here or in the sweep report.
- If AWS requests screenshot evidence, the most useful screenshots are:
  - IAM → Security credentials → root MFA enabled + password last changed date
  - EC2 → Security Groups (any region) showing `sec-hhs` is gone
  - CloudTrail → Event history → `ImportKeyPair` event detail (shows `AKIAIQYX7Z65F25NMY6A` + source IP `45.61.128.156`)
