# AWS case 177613748700177 — consolidated reply + resource disposition (May 27 2026)

**Account:** `767697632458` (Nelanco)
**Case:** `177613748700177`
**Prepared by:** incident-response automation (live read-only sweep) + operator review
**Prior reports:** `2026-04-14-case-...response.md`, `2026-04-15-final-account-sweep-...md`, `2026-04-18-followup-sweep-...md`, `2026-04-18-case-...followup-reply.md`

---

## 0. Why this case is still open (diagnosis)

The case keeps auto-nudging ("we have not heard back…") for **procedural**, not
security, reasons:

1. **Apr 30 (Mafu T.)** gave an explicit resource list to *confirm or delete*
   (IAM user `database_backup`, two SSO roles, 3 security groups, key pairs,
   39 AMIs, 4 snapshots). Our **May 11 reply only re-addressed security groups** —
   it never explicitly confirmed the AMIs, snapshots, IAM user, or roles. AWS is
   waiting on that confirmation.
2. **Apr 23 (Rashmi A.)** flagged that our replies came from an **IAM / SSO role**,
   and asked the **root account holder** to reply using **root** sign-in. Replies
   since then still need to come from root.
3. **May 20 (Luis)** narrowed the open asks to two questions: (a) the location
   (country/city) the password was reset from + any VPN; (b) a list of other
   users (IAM / IT) with access, and their locations.

This reply closes all three at once. **Send it signed in as the root user.**

---

## 1. Live read-only inventory (swept 2026-05-27)

Generated by `scripts/aws/inventory_account.py --account nelanco` (Describe* only).

| Region | Instances | Key pairs | Self AMIs | Self snapshots | Non-default SGs |
|--------|-----------|-----------|-----------|----------------|-----------------|
| us-east-1 | 13 | 7 | 39 | 56 | 3 |
| ap-southeast-1 | 1 | 1 | 0 | 0 | 1 |
| us-west-1 | 1 | 1 | 0 | 0 | 0 |
| **Total** | **15** | **9** | **39** | **56** | **4** |

**Root / IAM posture:** `AccountMFAEnabled = true`, `AccountAccessKeysPresent = 0`
(no root access keys), 5 IAM users. The compromised root key
`AKIAIQYX7Z65F25NMY6A` remains absent.

### 1.1 Reconciliation against the Apr 30 (Mafu T.) list

| AWS-flagged item | Live finding | Disposition |
|---|---|---|
| IAM user `database_backup` | Created 2026-04-17 by operator; key last used 2026-05-10 (EC2, us-east-1) | **Authorized** — created post-incident to run the monthly EBS-snapshot automation (scoped to EC2 only) |
| Role `AWSReservedSSO_AdministratorAccess_ab752baaa9a0c7c1` | Present | **Authorized** — IAM Identity Center (SSO) permission set, operator-provisioned |
| Role `AWSServiceRoleForSSO` | Present | **Authorized** — AWS service-linked role for IAM Identity Center |
| Security groups: 3 in us-east-1 + ap-southeast-1 | us-east-1: `new default` (seni_redis_2), `launch-wizard-1`, `governor-chatbot-sg`; ap-southeast-1: 1 (LATOKENS exchange proxy) | **Authorized** — all attached to named production instances |
| Key pairs in us-east-1 | 7, all created 2018–2026 with operator names (GETDATA*, TrueSight*, NelanCoProduction, governor_chatbot) | **Authorized** — no attacker-pattern / region-named keys; IOC key `buatbelisdfgmsobilbaim` absent |
| 39 AMIs | All operator-named (`seni_*`, `krake_*`), dated 2021–2026, none on/around the Apr-13 incident date | **Authorized** — long-standing operator machine images |
| 4 snapshots → now 56 | All are AMI-backing snapshots ("Created by CreateImage…") or labeled DB backups ("Monthly backup of seni_sql/krake", "seni_sql_YYYYMMDD") | **Authorized** — count grew because the monthly backup automation (added 2026-04-17) has been running; none attacker-created |

**Conclusion: the account is clean.** Every instance, key pair, AMI, snapshot,
and non-default security group maps to a legitimate operator asset. No attacker
artifacts remain.

### 1.2 Current IAM users (answers Luis's "list of users" question)

| User | Created | Recent use | Notes |
|---|---|---|---|
| `database_backup` | 2026-04-17 | EC2, us-east-1 | Monthly snapshot automation (post-incident) |
| `edgar_aws_billing_automation` | 2025-06-29 | Cost Explorer, us-east-1 | Billing automation |
| `truesight_app` | 2022-07-17 | S3, us-east-1 | Application S3 access |
| `truesight_dao_autopilot` | 2026-05-03 | EC2 describe, multi-region | Read-only monitoring (post-incident; created after the Apr-30 list) |
| `truesight_model` | 2022-07-27 | none recorded | Legacy, idle |

All operate from the operator's infrastructure. No attacker-created IAM users.

---

## 2. Message to paste into case 177613748700177 (send as ROOT)

> **Operator: verify the bracketed location line before sending.** AWS explicitly
> asked the root holder to reply — sign in via root, not SSO/IAM.

```
Hello,

Replying as the root account holder for account 767697632458, case 177613748700177.
This consolidates confirmation of the resources you listed on Apr 30, and answers
the access questions from the May 20 follow-up.

────────────────────────────────────────────
1. RESOURCES FROM YOUR APR 30 LIST — ALL AUTHORIZED
────────────────────────────────────────────

We reviewed every item and confirm each is an authorized, operator-owned resource:

- IAM user "database_backup": created by us on 2026-04-17 to run our monthly EBS
  snapshot automation. Its access is scoped to EC2 only. Authorized.

- IAM roles "AWSReservedSSO_AdministratorAccess_ab752baaa9a0c7c1" and
  "AWSServiceRoleForSSO": these are our IAM Identity Center (AWS SSO) permission
  set and its service-linked role. Operator-provisioned. Authorized.

- Security groups (us-east-1 and ap-southeast-1): the remaining non-default
  groups are "new default", "launch-wizard-1", and "governor-chatbot-sg" in
  us-east-1, plus the LATOKENS exchange-proxy group in ap-southeast-1. All are
  attached to named, in-service production instances. Authorized. (The
  attacker-planted "sec-hhs" groups across 16 regions and the orphaned
  launch-wizard groups were deleted on 2026-04-18, as previously reported.)

- Key pairs (us-east-1): 7 key pairs, all created between 2018 and 2026 under our
  own naming (GETDATA_*, TrueSight DAO, NelanCoProduction, governor_chatbot).
  None match the attacker pre-staging pattern. The IOC key
  "buatbelisdfgmsobilbaim" is absent. Authorized.

- AMIs (39) and EBS snapshots: all are our own machine images and their backing
  snapshots, plus dated database backups ("Monthly backup of seni_sql/krake",
  "seni_sql_YYYYMMDD"). They are named under our long-standing operator
  conventions and date from 2021 through 2026; none coincide with the incident
  date. Authorized. (The snapshot count is higher than 4 because our monthly
  backup automation has continued running since the incident.)

We did not find any unauthorized AMIs, snapshots, instances, key pairs, IAM
users, roles, or security groups remaining in the account.

────────────────────────────────────────────
2. ACCOUNT ACCESS DETAILS (per your May 20 request)
────────────────────────────────────────────

- Location of password reset / account access: San Francisco, California, USA.
  We do not use a VPN for routine root or console access.

- Users with access to the account and their locations (all operate from our own
  infrastructure; no third parties):
    * Root holder (Gary Teh) — San Francisco, California, USA
    * IAM user "edgar_aws_billing_automation" — billing automation, us-east-1
    * IAM user "truesight_app" — application S3 access, us-east-1
    * IAM user "database_backup" — snapshot automation (created 2026-04-17), us-east-1
    * IAM user "truesight_dao_autopilot" — read-only monitoring (created 2026-05-03),
      runs from our EC2 in us-east-1
    * IAM user "truesight_model" — legacy, currently idle
  No IAM users were created by the attacker.

────────────────────────────────────────────
3. CREDENTIAL POSTURE — CONFIRMED
────────────────────────────────────────────

- Root MFA: enabled and active.
- Root access keys: none present (0).
- Compromised key AKIAIQYX7Z65F25NMY6A: confirmed removed; not active anywhere.
- Root password: changed during remediation and most recently rotated 2026-05-15.

We believe the account is fully remediated and all listed resources are confirmed
authorized. We would appreciate confirmation that the temporary service
limitations can be lifted and that this case can be closed. Please let us know if
any further evidence (e.g. console screenshots) would help.

Thank you,
Gary Teh
GetData Inc / TrueSight DAO
Account: 767697632458
```

---

## 3. After sending

- **Sent: 2026-05-27 ~11:14 PDT** — operator posted the §2 reply into case
  `177613748700177` while signed in as the root user.
- If AWS asks for screenshot evidence, the highest-value captures are:
  - IAM → root: MFA enabled + "no root access keys" + password last-changed date
  - EC2 → AMIs (us-east-1) showing the operator naming convention
  - EC2 → Snapshots showing the "Created by CreateImage…" / "Monthly backup…" descriptions
- Optional hygiene (not required for the case): deactivate the older idle access
  key on `truesight_dao_autopilot` (`AKIA3FPSYHTFFH4TCZUA`, last used 2026-05-03);
  set the `SECURITY` alternate contact (still missing per prior sweeps).

---

## 4. Disclaimer

Operator-focused snapshot from API-visible state at sweep time (read-only Describe*
calls via `scripts/aws/inventory_account.py`). Continued CloudTrail / GuardDuty
monitoring recommended.
