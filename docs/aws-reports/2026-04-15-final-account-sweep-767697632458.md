# AWS account 767697632458 - final sweep report (post-incident)

**Date (UTC):** 2026-04-16  
**Account:** `767697632458`  
**Prepared by:** incident-response automation + operator review  
**Related support references:** `177613748700177`, `177611287600671`  
**Related repo docs:** `docs/aws-reports/2026-04-14-case-177613748700177-account-security-response.md`, `docs/incidents/2026-04-13-aws-ec2-trust-safety-abuse.md`

---

## 1) Scope of this sweep

This sweep re-checked the main areas AWS asked to verify after the credential-misuse incident:

1. Root / IAM credential safety.
2. CloudTrail anomaly indicators.
3. Unexpected compute / key-pair artifacts across regions.
4. Common abuse surfaces (security groups, Spot requests, selected managed services).
5. Contact hygiene and billing sanity checks.

All commands were executed against account `767697632458` using refreshed temporary credentials.

---

## 2) Executive status

**Overall:** account is materially improved and the original IOC artifacts are removed, but there are still **hardening gaps** to close before claiming a fully clean baseline.

### Confirmed good

- Root MFA is active and root access keys are inactive.
- IOC key pair `buatbelisdfgmsobilbaim` is not present in any checked region.
- IOC instances (`i-058e106e505d11df1`, `i-023059e53e79e1dc3`) are not present.
- No active Spot requests/fleets found.
- No nonzero Lambda/ECS/Glue/SageMaker notebook inventory detected in this sweep.

### Still outstanding

- High-risk security-group rules remain (details in section 5), including:
  - world-open RDP ingress in `us-east-1`,
  - world-open broad ingress in `us-west-1`,
  - many world-open all-port egress defaults.
- `SECURITY` alternate contact is not configured.
- CloudTrail shows recent `CreateUser` events from root via `sso-directory.amazonaws.com`; likely administrative IAM Identity Center activity but should be explicitly validated against operator intent and timeline.

---

## 3) Root and IAM credential posture

Credential report snapshot (`<root_account>` row):

- `mfa_active`: `true`
- `password_enabled`: `true`
- `password_last_changed`: `2026-04-15T20:04:04Z`
- `access_key_1_active`: `false`
- `access_key_2_active`: `false`

IAM users currently present:

- `edgar_aws_billing_automation` (active key, last used CE in `us-east-1`)
- `truesight_app` (active key, last used S3 in `us-east-1`)
- `truesight_model` (active key, no recorded recent use)

Notes:

- The compromised root key from earlier incident context (`AKIAIQYX7Z65F25NMY6A`) is not active.
- Root key slots currently show inactive in credential report.

---

## 4) CloudTrail anomaly sweep (14-day window)

### IOC checks

- `ImportKeyPair`: one hit tied to known incident pattern:
  - source IP `45.61.128.156`
  - principal `arn:aws:iam::767697632458:root`
  - key `AKIAIQYX7Z65F25NMY6A`
- `RunInstances`: includes one hit from same IOC source/IP pattern; other entries primarily Auto Scaling service role activity.

### Additional events to verify

Recent successful `CreateUser` events were observed:

- event source: `sso-directory.amazonaws.com`
- principal type: root
- source IP: `23.125.222.164`
- timing: same operational period as account recovery

Interpretation:

- These may represent legitimate IAM Identity Center user provisioning.
- Because they are successful root-scope user creation actions, they should be reconciled in operator notes (who, why, expected usernames).

---

## 5) Regional resource and exposure findings

### 5.1 IOC artifact status

- Key pair `buatbelisdfgmsobilbaim`: **not found**
- Instance `i-058e106e505d11df1`: **not found**
- Instance `i-023059e53e79e1dc3`: **not found**

### 5.2 EC2 / key-pair inventory (nonzero regions)

Regions with nonzero EC2 instances and/or key pairs were found (normal for this account), including:

- `us-east-1`, `us-west-1`, `ap-southeast-1`, and several regions with key pairs only.

This is not by itself an anomaly; it establishes where hardening should focus.

### 5.3 Security group risk posture

High-risk rules still present, including examples:

- `us-east-1` `sg-0ac49f70367425682` (`launch-wizard-1`):
  - inbound TCP `3389` from `0.0.0.0/0` (world-open RDP)
- `us-west-1` `sg-c6eeaabd` (`default`):
  - inbound TCP `0-65535` and UDP `0-65535` from `0.0.0.0/0`
- multiple default / launch-wizard / sec-hhs groups:
  - world-open all-port egress (`IpProtocol=-1`)

Security-group findings count in this sweep: **48** high-risk entries (combining ingress and egress patterns as defined in the scan).

---

## 6) Service usage and billing quick checks

- Spot artifacts: none active.
- Lambda/ECS/Glue/SageMaker notebook object scan: zero nonzero regions in this check.
- S3 bucket inventory present (multiple buckets; expected for account history).
- Last-14-day cost snapshot shows notable spend in:
  - EC2 Compute,
  - EC2 Other,
  - VPC,
  - RDS.

No single billing line item in this quick check alone proves active abuse, but EC2 spend remains significant and should continue to be monitored.

---

## 7) Contact hygiene

- Primary account contact exists.
- `SECURITY` alternate contact is missing (`ResourceNotFoundException` on lookup).

Action: set a dedicated security contact to improve abuse-response routing.

---

## 8) Final remediation checklist (recommended immediate actions)

1. **Close SG exposure gaps**
   - Remove world-open RDP (`3389`) unless explicitly required and tightly source-restricted.
   - Remove broad world-open ingress in `us-west-1` default SG.
   - Review all world-open all-port egress rules and reduce to required destinations/ports.

2. **Validate `CreateUser` activity**
   - Confirm recent root-initiated `sso-directory.amazonaws.com` `CreateUser` events were intentional.
   - Record expected user identities and purpose in incident notes.

3. **Set SECURITY alternate contact**
   - Configure and verify in AWS account alternate contacts.

4. **Continue root minimization**
   - Keep root keys inactive (already true in this sweep).
   - Continue moving routine operations to least-privilege IAM + MFA.

5. **Support-case closure hygiene**
   - In AWS replies, state:
     - root key slots inactive,
     - IOC artifacts removed,
     - CloudTrail reviewed,
     - and mention SG hardening in progress/completed with specifics.

---

## 9) Disclaimer

This is an operator-focused snapshot based on API-visible state at sweep time. It reduces uncertainty but does not guarantee absence of all compromise paths. Maintain monitoring and periodic re-sweeps.

